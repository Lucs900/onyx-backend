# Post-link desk line after account consume (23 Sep 2026)

## Status
OPEN — tip Code Builder. Preview only. Do not merge. READY ≠ ACCEPT.
`start.onyxdirect.com` resume host stays CLOSED (43-account-prod-sender ACCEPT).

## Why
After `/start?account=…` is opened, the live Fox line must not still say “Check your email for a link to this File.” That is the mail-wait prompt; the desk is now the person’s.

## Locked targets (this SHA)
- After magic link consume: live Fox line is **not** “Check your email for a link to this File.”
- History may keep that sentence.
- Live line = **This File is yours.** + product chips when Structure is empty, **or** the next empty Structure ask when a product is already written.
- Do **not** reprint Create account / Where should I send the sign-in link.
- Header Log in parked (not this SHA). Pad Save-only parked (not this SHA).

## Keep closed
`start.onyxdirect.com` resume · `575fee8` letter-on-preview · `f845741` pad · `d0dd8ed` why · `4cc75b7` hub · `8fa7382` dense · `97e6b9b` chips

## Accept bar (founder paperclip)
1. InPrivate Create account → letter → second InPrivate opens the link.
2. Address cleans to `/start?path=acr`.
3. SHOT live line is **not** the mail prompt.
4. Same File (email may stay in history) · chips or next ask present.

FAIL if: live line still Check your email…; Create account / Where should I send reprinted; resume host / pad / why / hub rewritten; header Log in “fixed” as this SHA.
