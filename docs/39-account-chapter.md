# Account chapter (22 Sep 2026)

## Status
READY preview. Do not merge. READY ≠ ACCEPT. Walker-green ≠ founder-green.

## Why
After hub v0 spine, the File must follow the person across browsers via account — not “this device.” Invite-only is only a side door.

## Lock
- Account can start on the first question, not only after Proceed.
- Email magic link or phone code. Same person, same file_id on a second browser.
- Homepage `/start` resumes that File.
- Hub opens that `file_id`, not “this device.”
- foxLine already on the File is the next line when they return.
- Not now still allowed (browser sketch). Invited or public later is the same desk once they have an account.
- No Google-required. No SSN login. No BNTouch as source of truth. No public domain cutover. No invite-reward.

## Preview hooks (no secrets)
- Quiet chips on the first question: **Create account · Not now**. Live product chips stay Buy · Refinance · HELOC · Jumbo · Other.
- Email → Fox prints `/start?account=<token>`. Open that URL on another browser.
- Phone → Fox prints a 6-digit code. Open `/start?code=<digits>` or type the code.
- `/login` is the same desk resume, not a second product.

## Keep closed
4cc75b7 hub v0 spine, f896f95 Skip-on-name, and prior HELOC/borrower chapter.

## Out of this SHA
Dense 1008, holding-page publish, referral cash, credit pull.

## Accept bar
Create account on first question → open `/start` in a second browser with the link/code → notepad + last Fox line match the first browser. Proceed still works → in_queue.

Tip SHA + unique READY preview + bypass when READY. READY ≠ ACCEPT. Do not call ACCEPT.
