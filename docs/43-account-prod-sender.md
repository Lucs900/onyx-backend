# Production sender + cookie-less resume host (23 Sep 2026)

## Status
OPEN — tip Code Builder. Preview only. Do not merge. READY ≠ ACCEPT.
`575fee8` letter-on-preview stays locked (preview protection-bypass path). This SHA is deliver + resume host only.

## Why
Borrower Create account on the first Save wall must deliver a real magic link and resume the same File in InPrivate without a Vercel login wall or protection-bypass on the link they tap.

## Locked targets (this SHA)
- **From:** ONYX Direct &lt;lucas@onyxdirect.com&gt;
- **Subject:** Your ONYX File
- Create account on first Save wall delivers the magic link.
- No `account=` token in the Fox thread.
- InPrivate Open your desk: same File, Fox + pad, no Vercel login wall, **no protection-bypass required** on the borrower-tapped link.
- Resume host is clean `/start?path=acr` — not a raw `vercel.app` preview souvenir URL.

## Hosting (probed — do not auto-prefer production)
- Unique preview + branch alias: Vercel Authentication (SSO). `575fee8` bypass stays for those origins.
- `onyx-backend-ten.vercel.app`: public, no `/start` (2346d7e). Never auto-prefer.
- `onyxdirect.com` / `onyxlending.com`: not this desk.
- Cookie-less letter host = `ACCOUNT_RESUME_ORIGIN` / `ONYX_RESUME_ORIGIN` when that host is public. Blob can resume the File created on the unique preview.

## Keep (do not rewrite)
- Design door copy · Save why-sentence (`d0dd8ed`) · Fox chips (`97e6b9b`) · pad Save only (`f845741`)
- No intelligence rewrite · No hub work

## Keep closed
`f845741` pad · `d0dd8ed` why · `97e6b9b` chips · `8fa7382` · `4cc75b7` · `575fee8` letter-on-preview

## Out of this SHA
Header Log in / Start your relationship leftover · holding page · dense grid

## Accept bar (founder paperclip)
1. HELOC 500 / 400 / 50 → Proceed → Create account → email `lucas@onyxlending.com`.
2. Letter: From ONYX Direct &lt;lucas@onyxdirect.com&gt; · Subject Your ONYX File · no token spoken in Fox thread.
3. InPrivate Open your desk → same `file_id` · gathering or preparing · line `$50k` / `$367`.
4. Link needs no protection-bypass; no Vercel login wall; address cleans to `/start?path=acr`.

FAIL if: stub sender; token in thread; InPrivate Vercel login; bypass required on borrower link; raw preview souvenir host; chips/why/pad rewritten.
