import { PlaceholderPage } from "@/components/PlaceholderPage";

export default function LoginPage() {
  return (
    <PlaceholderPage
      title="Log in"
      note="Returning clients will be chat-first. Dashboard and login are not in this slice."
    />
  );
}
